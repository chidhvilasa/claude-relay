## Open the Relay Dashboard

The dashboard shows this project's real state at a glance: protection status, the latest checkpoint and
handoff, and repository info — no fake percentages, no guessed values.

[Open Dashboard](command:claudeRelay.openDashboard)

That's the whole workflow. From here: `Claude Relay: Resume Previous Task` to recover, `Claude Relay:
Show Logs` to troubleshoot, and `Claude Relay: Remove Claude Integration` if you ever need to clean up
legacy v0.1 hooks.
