## Create Your First Checkpoint

A checkpoint is a deterministic snapshot of your repository — branch, commit, and dirty state — written
to `.relay/checkpoints/`. It doesn't need Claude authentication and doesn't touch Git.

[Create Checkpoint](command:claudeRelay.checkpoint)

Open the `.relay/checkpoints/` folder in your project afterward to see the real file it wrote.
